jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      
      # Corrected Build with Jekyll step: using defaults
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        # Removing the 'with' block uses the correct defaults: 
        # source: ./ (repository root) and destination: _site
      
      # Corrected Upload artifact step: uploading the _site directory
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site # <-- ✅ CORRECT: Upload the entire built site directory
